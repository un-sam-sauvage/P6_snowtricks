{{ form_start(form) }}
{# Ca ne me convient pas à voir ce que l'on peut faire de mieux #}
	<div class="form-group">
	{{ form_label(form.name, "Name") }}
	{{ form_widget(form.name) }}
	</div>
	<div class="form-group">
	{{ form_label(form.content, "Content") }}
	{{ form_widget(form.content) }}
	</div>
	<div class="form-group">
	{{ form_label(form.categories, "Categories") }}
	{{ form_widget(form.categories) }}
	</div>
	<div class="form-group">
	{{ form_label(form.author, "Author") }}
	{{ form_widget(form.author) }}
	</div>
	<div class="form-group">
	{{ form_label(form.media, "Media") }}
	{{ form_widget(form.media) }}
	</div>
	<div class="form-group">
	{{ form_label(form.video, "Video") }}
	{{ form_widget(form.video.vars.prototype) }}
	</div>
	<button class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
